[package]
name = "ot-plugin-state"
version = "0.1.0"
edition = "2021"
description = "Compatibility shim for legacy OTools plugin state"

[dependencies]
ot-storage = { path = "../ot-storage" }
otools-core = { path = "../../core" }
otools-plugin-state = { path = "../../platform/plugin-state" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
