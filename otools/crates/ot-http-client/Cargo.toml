[package]
name = "ot-http-client"
version = "0.1.0"
edition = "2021"
description = "Compatibility shim for legacy OTools HTTP client"

[dependencies]
otools-core = { path = "../../core" }
otools-platform-http-client = { path = "../../platform/http-client" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
