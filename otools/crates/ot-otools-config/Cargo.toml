[package]
name = "ot-otools-config"
version = "0.1.0"
edition = "2021"
description = "Compatibility shim for legacy OTools global config helpers"

[dependencies]
ot-storage = { path = "../ot-storage" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
