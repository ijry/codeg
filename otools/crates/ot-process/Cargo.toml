[package]
name = "ot-process"
version = "0.1.0"
edition = "2021"
description = "Compatibility shim for legacy OTools process helpers"

[dependencies]
otools-platform-process = { path = "../../platform/process" }
